services:
  cardboard:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - cardboard-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production

volumes:
  cardboard-data:
