services:
  cardcanvas:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - cardcanvas-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production

volumes:
  cardcanvas-data:
